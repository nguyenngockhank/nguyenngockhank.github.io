{
  "scripts": {
    "dev-notes": "cd notes && npm run dev",
    "build": "cd notes && npm run build && cd .. && git add docs && git commit -m \"build\"",
    "push": "git push"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "socket.io": "^4.6.2"
  }
}
